extends layout

block content
  h1 Name: #{pokemon.name}

  p #[strong Type(s):] #{pokemon.type}
  p #[strong Pokedex Entry:] #{pokemon.description}
  p #[strong Number:] #{pokemon.Number}

  hr
  p
    a(href=pokemon.url+'/delete') Delete Pokemon
  p
    a(href=pokemon.url+'/update') Update Pokemon